# 个人简历

> 后端开发工程师 · Go 技术栈 · 物联网 / 微服务方向

---

## 个人信息

- **姓名：** [你的姓名]
- **求职意向：** 后端开发工程师（Go）
- **工作年限：** [X] 年
- **联系方式：** [手机号] / [邮箱]
- **GitHub：** [链接]

---

## 技能栈

- **编程语言：** Go（主力）、SQL
- **Web 框架：** Gin、GoFrame v2
- **RPC / 通信：** gRPC、Protobuf、RESTful API
- **数据库 / 缓存：** MySQL（InnoDB）、Redis、SQLite
- **消息中间件：** RabbitMQ、MQTT（paho）
- **IoT / 网络：** TCP 长连接、KCP 可靠 UDP（kcp-go）、485 串口、RTSP、WebRTC（pion）
- **服务治理：** Etcd 服务注册与发现
- **中间件 / 工具：** Viper、Zap、Cron、Prometheus、Cobra、Excelize
- **认证 / 权限：** JWT、RBAC 权限模型
- **存储 / 云：** 阿里云 OSS / 容器镜像服务 ACR、Docker 容器化、FRP 内网穿透
- **容器编排：** Kubernetes（Deployment / Service / ConfigMap / Secret / imagePullSecret）、Kustomize（base + overlay 多环境管理）、**Docker Compose** 多容器编排（含 GPU 设备调度、健康检查、profile 按需启停）
- **CI/CD：** GitHub Actions（代码检查 / 单元测试 / 多阶段镜像构建推送）、多阶段 Docker 构建
- **其他语言：** Python（FastAPI）、Node.js（Vue 3 / Nginx）
- **AI Agent / 辅助工具：** Codex CLI、OpenCode CLI、Claude Code、Cursor，熟练借助 AI 编程助手提升编码、调试与文档生成效率
- **第三方集成：** 支付宝开放平台、微信小程序、法大大电子签章、短信服务

---

## 项目经历

### 一、小区停车计费管理系统 ｜ 后端开发 ｜ [起始时间] - [结束时间]

**技术栈：** Go / Gin / GORM / gRPC + Protobuf / gorilla/websocket / pion/webrtc / Viper / Zap / kcp-go / MySQL

**项目简介：** 面向住宅小区的车辆进出管理与停车计费系统，采用微服务架构，拆分为 API、设备控制、计费、数据库、WebSocket、云端管理、RTSP 视频流等 9 个独立服务，分别编译部署，服务间通过 gRPC 跨服务调用，支持管理员后台与保安端双操作体系。

**主要工作：**
- 微服务拆分：将系统拆分为 9 个独立服务（各自独立 `config`/`core`/`global` 模块），定义 8 个 proto 文件（约 4000 行），通过 gRPC + Keepalive 实现服务间高效通信。
- 基于 **Gin + GORM** 构建 RESTful API，实现 JWT 鉴权与 RBAC 三级权限（用户-角色-菜单），区分管理员/保安双角色路由，支持 CORS、Excel 导入导出。
- 设计复杂计费引擎，支持按时段/按天/分时段/夜间差异化等多策略计费，包含优惠券与商户活动体系，支持模拟计费调试。
- 基于 **gorilla/websocket** 的 Hub 模式实现实时消息推送，按频道分组广播进出场状态与闸机事件至监控大屏与保安端。
- 集成 **RTSP + WebRTC（pion）** 实现摄像头视频流接入与浏览器端实时观看；基于 **TCP/KCP 长连接 + 485 串口** 与道闸、LED 播报板等硬件设备通信。
- 使用 **Viper + fsnotify** 配置热加载、**Zap** 高性能日志、**Prometheus** 监控；Shell 脚本一键编译 9 服务，支持交叉编译与 FRP 内网穿透部署。

---

### 二、二轮电瓶车换电平台 ｜ 后端开发 ｜ [起始时间] - 至今

**技术栈：** GoFrame v2 / gRPC / MySQL / Redis / RabbitMQ / MQTT / Etcd / Docker / 阿里云 OSS

**项目简介：** 面向二轮电瓶车换电场景的物联网平台，基于 Golang 微服务架构，管理数千台换电柜设备，为 C 端用户与企业客户提供换电套餐购买、电池更换调度、运维工单等完整闭环服务，支撑日均千级换电柜设备在线管理与调度。

**主要工作：**
- 基于 **TCP + MQTT 双协议** 实现换电柜设备接入层，涵盖设备注册、心跳保活、远程控柜、状态上报等完整链路，设计报文解析框架支持 10+ 种消息类型及断线重连。
- 独立开发 **BMS 电池管理服务**，通过 TCP 接收电池实时数据，利用 RabbitMQ 实现设备数据异步消费与业务解耦，保障高并发下的消息可靠性与入库幂等。
- 设计企业骑手套餐计费引擎，实现套餐额度管理、到期自动计算与逾期冻结逻辑，通过 Cron 定时任务（Redis 分布式锁）保障日级数据统计准确性。
- 基于 **JWT + Redis** 实现多端（账号/微信/支付宝）统一登录与权限管理，集成法大大电子签章完成企业用户在线签约全链路。
- 采用 **GoFrame 微服务架构** 拆分用户、柜体、BMS、工单、定时任务等独立服务，通过 Etcd 注册发现 + gRPC 通信，Docker 容器化部署；基于 RabbitMQ 发布/订阅构建统一通知中心。

---

### 三、Star 投资数据服务（GoFrame 单体服务） ｜ 后端开发 ｜ [起始时间] - [结束时间]

**技术栈：** GoFrame v2 / MySQL / Kubernetes / Kustomize / GitHub Actions / Docker / 阿里云 ACR / excelize

**项目简介：** 基于 GoFrame v2 单体仓库（SingleRepo）架构的投资数据管理与分析服务，提供行情数据抓取、基金组合分析、REIT 等接口能力；项目完整落地容器化交付链路，基于 Kubernetes 编排部署，并通过 GitHub Actions 实现 CI/CD 自动化。

**主要工作：**
- 基于 **Kustomize（base + overlay）** 管理 K8s 资源，编写 Deployment（3 副本）、NodePort Service、ConfigMap、Secret 等清单，配置阿里云 ACR 私有仓库 `imagePullSecret` 与配置/日志卷挂载，支持多环境差异化部署。
- 搭建 **GitHub Actions CI/CD 流水线**：push 到 main/develop 或打 `v*` tag 时触发，先执行 `go test` 单元测试与 `go build` 编译，通过后多阶段构建 Linux/amd64 镜像并推送至阿里云镜像仓库（按 commit SHA + 版本号双标签）。
- 编写 **多阶段 Dockerfile**（Go 1.24 编译镜像 → alpine 运行镜像），实现 `CGO_ENABLED=0` 静态编译与镜像体积精简。
- 通过 `kubectl rollout` 实现滚动更新、回滚与副本扩缩容，沉淀 K8s 日常排障（get/logs/describe）与配置热更新实践。

---

### 四、储能文件分析 / 异步资料核验系统 ｜ 后端开发 ｜ [起始时间] - [结束时间]

**技术栈：** FastAPI（Python） / Vue 3 + Element Plus / Docker Compose / Nginx / SQLite / Gotenberg / MinerU OCR（GPU） / PaddlePaddle

**项目简介：** 面向储能项目尽调的异步资料核验系统，用户上传客户资料压缩包后，后端异步完成解压、文本提取、OCR、AI/规则分类，并输出核验清单与关键参数。系统涵盖 FastAPI 后端、Vue 3 前端，并通过 Docker Compose 一键编排后端、前端、文档转换与 OCR 等多服务。

**主要工作：**
- 基于 **Docker Compose** 编排 `backend`（FastAPI）、`frontend`（Vue 3 + Nginx）、`gotenberg`（LibreOffice 文档转 PDF）、`mineru`（OCR）多个服务，使用自定义 bridge 网络 `app-net` 互联，前端经 Nginx 反向代理 `/api/*` 访问后端。
- 配置容器 `healthcheck` 与 `depends_on: service_healthy` 控制启动依赖顺序，结合 `restart: unless-stopped` 保障服务自愈；后端通过 `env_file` + `environment` 注入配置，挂载 `storage` 卷实现文件持久化。
- 引入 **GPU 设备调度**：在 compose 中通过 `deploy.resources.reservations.devices`（nvidia driver + device_ids）为 MinerU OCR 服务分配 GPU，并支持 `profiles` 按需启停（`--profile gpu` 启用 OCR，无 GPU 场景仅启动主链路）。
- 编写多阶段构建脚本（`build_and_push.sh`）分别构建后端（Python 3.10 slim + PaddlePaddle + venv）与前端（node:20 → nginx 多阶段）镜像并推送至私有仓库，配合 `compose.yaml up -d` 完成一键部署与滚动更新。

---

## 教育背景

- [学校名称] · [专业] · [学历] ｜ [入学年份] - [毕业年份]

---

## 个人总结

- 具备扎实的 Go 后端开发能力，熟悉微服务架构设计与 gRPC / Protobuf 服务间通信。
- 有丰富的物联网（IoT）设备接入经验，熟悉 TCP 长连接管理、MQTT、KCP 可靠传输及硬件设备通信。
- 熟悉高并发场景下的 Redis 原子操作、消息队列异步解耦与分布式锁实践。
- 具备计费引擎、权限模型、通知中心等业务模块的设计与落地能力。
